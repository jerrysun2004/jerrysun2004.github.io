#include <complex>
#include <queue>
#include <set>
#include <unordered_set>
#include <list>
#include <chrono>
#include <random>
#include <iostream>
#include <algorithm>
#include <cmath>
#include <string>
#include <vector>
#include <map>
#include <unordered_map>
#include <stack>
#include <iomanip>
#include <fstream>
 
using namespace std;
 
typedef long long ll;
typedef long double ld;
typedef pair<int,int> p32;
typedef pair<ll,ll> p64;
typedef pair<double,double> pdd;
typedef vector<ll> v64;
typedef vector<int> v32;
typedef vector<vector<int> > vv32;
typedef vector<vector<ll> > vv64;
typedef vector<vector<p64> > vvp64;


typedef vector<p64> vp64;

typedef vector<p32> vp32;

ll MOD = 998244353;

double eps = 1e-12;

#define forn(i,e) for(ll i = 0; i < e; i++)


#define forsn(i,s,e) for(ll i = s; i < e; i++)


#define rforn(i,s) for(ll i = s; i >= 0; i--)
#define rforsn(i,s,e) for(ll i = s; i >= e; i--)
#define ln "\n"
#define dbg(x) cout<<#x<<" = "<<x<<ln
#define mp make_pair
#define pb push_back
#define fi first
#define se second
#define INF 2e18
#define fast_cin() ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)
#define all(x) (x).begin(), (x).end()
#define sz(x) ((ll)(x).size())
 set<int> si;
 map<int,int> mip, mid;
 vector<vector<int> > vii;

//Unity looks like it might be kind of fun
//it is going to be interesting learning how to use it.
 //competitive programming is a lot about if statements and while loops and for loops
 // just learn to get better at those and I will be doing good
 // recursion like three sum in leet code and combination will also do me wonders
 // learn to see the edge cases that causes the segmentation faults and memory leaks will also help boost my confidence
 
int a[n][n];
array<int,2> a;

void solve(){
	int n; cin >> n;
              for (int j=0; j<m; j++){
                
    for (int i=0: i<n; i++){


        
                    
    }
        
    }
	for (int i=1; i<n; i++)
		cout << i << "\t" endl;
	cout << n << "\n";
	vector<int> vi;

	//learning how to code is going to be tough but something I will have to learn to do

}
int dfs(int x){ 
    
  
}
int main(){
    fast_cin();
    ll t;
    cin >> t;
    for(int it=1;it<=t;it++) {
    
        solve();
    }
    return 0;
}